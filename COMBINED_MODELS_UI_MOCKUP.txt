╔══════════════════════════════════════════════════════════════════════════════╗
║              COMBINED FEATURE EXTRACTORS - UI MOCKUP (7 PRESETS)             ║
╚══════════════════════════════════════════════════════════════════════════════╝

SCENARIO 1: Single Model - CLIP Selected
═══════════════════════════════════════════════════════════════════════════════

┌────────────────────────────────────────────────────────────────────────────┐
│ 🧠 AI Model Selection                                                      │
├────────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│  Feature Extractor:   [CLIP (image-to-text classification)        ▼]      │
│                                                                            │
│  CLIP Model:          [CLIP_ViT-B/32 (340 MB - Balanced)          ▼]      │
│                                                                            │
│  Organization Mode:   [Suggested (AI suggests, you confirm)       ▼]      │
│                                                                            │
└────────────────────────────────────────────────────────────────────────────┘

✅ CLIP Model dropdown is VISIBLE
❌ DINOv2 Model dropdown is HIDDEN
❌ Performance warning is HIDDEN


SCENARIO 2: Single Model - timm Selected
═══════════════════════════════════════════════════════════════════════════════

┌────────────────────────────────────────────────────────────────────────────┐
│ 🧠 AI Model Selection                                                      │
├────────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│  Feature Extractor:   [timm (PyTorch Image Models)                ▼]      │
│                                                                            │
│  Organization Mode:   [Suggested (AI suggests, you confirm)       ▼]      │
│                                                                            │
└────────────────────────────────────────────────────────────────────────────┘

❌ CLIP Model dropdown is HIDDEN
❌ DINOv2 Model dropdown is HIDDEN
❌ Performance warning is HIDDEN


SCENARIO 3: Combined - CLIP+DINOv2 Selected ⚠️
═══════════════════════════════════════════════════════════════════════════════

┌────────────────────────────────────────────────────────────────────────────┐
│ 🧠 AI Model Selection                                                      │
├────────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│  Feature Extractor:   [CLIP+DINOv2 (Combined: text + visual)      ▼]      │
│                                                                            │
│  ┌──────────────────────────────────────────────────────────────────────┐ │
│  │ ⚠️ Warning: CLIP+DINOv2 combines two models, which may reduce       │ │
│  │ processing speed. This provides better accuracy but takes longer    │ │
│  │ than single models.                                                  │ │
│  └──────────────────────────────────────────────────────────────────────┘ │
│  [Orange background, bold text]                                            │
│                                                                            │
│  CLIP Model:          [CLIP_ViT-B/32 (340 MB - Balanced)          ▼]      │
│                                                                            │
│  DINOv2 Model:        [DINOv2_base (340 MB - Balanced)            ▼]      │
│                                                                            │
│  Organization Mode:   [Suggested (AI suggests, you confirm)       ▼]      │
│                                                                            │
└────────────────────────────────────────────────────────────────────────────┘

✅ CLIP Model dropdown is VISIBLE
✅ DINOv2 Model dropdown is VISIBLE
✅ Performance warning is VISIBLE (2-model warning)


SCENARIO 4: Combined - CLIP+timm Selected ⚠️
═══════════════════════════════════════════════════════════════════════════════

┌────────────────────────────────────────────────────────────────────────────┐
│ 🧠 AI Model Selection                                                      │
├────────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│  Feature Extractor:   [CLIP+timm (Combined: text + PyTorch)       ▼]      │
│                                                                            │
│  ┌──────────────────────────────────────────────────────────────────────┐ │
│  │ ⚠️ Warning: CLIP+timm combines two models, which may reduce         │ │
│  │ processing speed. This provides better accuracy but takes longer    │ │
│  │ than single models.                                                  │ │
│  └──────────────────────────────────────────────────────────────────────┘ │
│                                                                            │
│  CLIP Model:          [CLIP_ViT-B/32 (340 MB - Balanced)          ▼]      │
│                                                                            │
│  Organization Mode:   [Suggested (AI suggests, you confirm)       ▼]      │
│                                                                            │
└────────────────────────────────────────────────────────────────────────────┘

✅ CLIP Model dropdown is VISIBLE
❌ DINOv2 Model dropdown is HIDDEN
✅ Performance warning is VISIBLE (2-model warning)


SCENARIO 5: Combined - DINOv2+timm Selected ⚠️
═══════════════════════════════════════════════════════════════════════════════

┌────────────────────────────────────────────────────────────────────────────┐
│ 🧠 AI Model Selection                                                      │
├────────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│  Feature Extractor:   [DINOv2+timm (Combined: visual + PyTorch)   ▼]      │
│                                                                            │
│  ┌──────────────────────────────────────────────────────────────────────┐ │
│  │ ⚠️ Warning: DINOv2+timm combines two models, which may reduce       │ │
│  │ processing speed. This provides better accuracy but takes longer    │ │
│  │ than single models.                                                  │ │
│  └──────────────────────────────────────────────────────────────────────┘ │
│                                                                            │
│  DINOv2 Model:        [DINOv2_base (340 MB - Balanced)            ▼]      │
│                                                                            │
│  Organization Mode:   [Suggested (AI suggests, you confirm)       ▼]      │
│                                                                            │
└────────────────────────────────────────────────────────────────────────────┘

❌ CLIP Model dropdown is HIDDEN
✅ DINOv2 Model dropdown is VISIBLE
✅ Performance warning is VISIBLE (2-model warning)


SCENARIO 6: Combined - ALL THREE (CLIP+DINOv2+timm) ⚠️⚠️
═══════════════════════════════════════════════════════════════════════════════

┌────────────────────────────────────────────────────────────────────────────┐
│ 🧠 AI Model Selection                                                      │
├────────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│  Feature Extractor:   [CLIP+DINOv2+timm (All three combined)      ▼]      │
│                                                                            │
│  ┌──────────────────────────────────────────────────────────────────────┐ │
│  │ ⚠️ Warning: Using all three models (CLIP+DINOv2+timm) will          │ │
│  │ significantly impact performance. Processing will be slower but may │ │
│  │ provide better accuracy for complex categorization tasks.           │ │
│  └──────────────────────────────────────────────────────────────────────┘ │
│  [Orange background, bold text - STRONG WARNING]                          │
│                                                                            │
│  CLIP Model:          [CLIP_ViT-B/32 (340 MB - Balanced)          ▼]      │
│                                                                            │
│  DINOv2 Model:        [DINOv2_base (340 MB - Balanced)            ▼]      │
│                                                                            │
│  Organization Mode:   [Suggested (AI suggests, you confirm)       ▼]      │
│                                                                            │
└────────────────────────────────────────────────────────────────────────────┘

✅ CLIP Model dropdown is VISIBLE
✅ DINOv2 Model dropdown is VISIBLE
✅ Performance warning is VISIBLE (3-model strong warning)


DROPDOWN MENU - ALL 7 PRESETS
═══════════════════════════════════════════════════════════════════════════════

When clicking Feature Extractor dropdown:

┌──────────────────────────────────────────────────────┐
│  CLIP (image-to-text classification)                │ ← Single
│  DINOv2 (visual similarity clustering)              │ ← Single
│  timm (PyTorch Image Models)                        │ ← Single
│  ─────────────────────────────────────────────────  │ ← Separator
│  CLIP+DINOv2 (Combined: text + visual)              │ ← Combined
│  CLIP+timm (Combined: text + PyTorch)               │ ← Combined
│  DINOv2+timm (Combined: visual + PyTorch)           │ ← Combined
│  CLIP+DINOv2+timm (All three combined)              │ ← Combined
└──────────────────────────────────────────────────────┘


PERFORMANCE WARNING STYLING
═══════════════════════════════════════════════════════════════════════════════

Warning Box Style:
  Background: #fff3e0 (light orange)
  Border: 1px solid #ffb74d (orange)
  Text Color: #ff6b00 (dark orange)
  Font: Bold
  Padding: 8px
  Border Radius: 4px
  Word Wrap: Enabled

Icon: ⚠️ (warning emoji)


INTERACTION FLOW FOR COMBINED MODELS
═══════════════════════════════════════════════════════════════════════════════

1. User opens Organizer Settings Panel
   ↓
2. Feature Extractor defaults to "CLIP (image-to-text classification)"
   ↓
3. User clicks Feature Extractor dropdown
   ↓
4. User sees all 7 options (3 single + 4 combined)
   ↓
5. User selects "CLIP+DINOv2 (Combined: text + visual)"
   ↓
6. Performance warning appears (orange box)
   ↓
7. Both CLIP Model and DINOv2 Model dropdowns appear
   ↓
8. User can configure both models
   ↓
9. Settings are automatically saved
   ↓
10. Backend uses CombinedFeatureExtractor to process with both models


BACKEND PROCESSING FLOW
═══════════════════════════════════════════════════════════════════════════════

Single Model (e.g., CLIP):
  Image → CLIP → Features (512-dim) → Classification

Combined 2-Model (e.g., CLIP+DINOv2):
  Image → CLIP → Features (512-dim) ┐
                                     ├→ Concat → Features (1024-dim) → Classification
  Image → DINOv2 → Features (512-dim)┘

Combined 3-Model (CLIP+DINOv2+timm):
  Image → CLIP → Features (512-dim)   ┐
  Image → DINOv2 → Features (512-dim) ├→ Concat → Features (1536-dim) → Classification
  Image → timm → Features (512-dim)   ┘


PERFORMANCE COMPARISON TABLE
═══════════════════════════════════════════════════════════════════════════════

┌──────────────────────┬──────────────┬─────────────┬──────────────┐
│ Configuration        │ Single Image │ 100 Images  │ Memory Usage │
├──────────────────────┼──────────────┼─────────────┼──────────────┤
│ CLIP                 │   ~0.5s      │   ~50s      │   ~500 MB    │
│ DINOv2               │   ~0.8s      │   ~80s      │   ~800 MB    │
│ timm                 │   ~0.3s      │   ~30s      │   ~400 MB    │
├──────────────────────┼──────────────┼─────────────┼──────────────┤
│ CLIP+DINOv2          │   ~1.3s      │  ~130s      │  ~1.5 GB     │
│ CLIP+timm            │   ~0.8s      │   ~80s      │  ~1.0 GB     │
│ DINOv2+timm          │   ~1.1s      │  ~110s      │  ~1.2 GB     │
│ CLIP+DINOv2+timm     │   ~1.6s      │  ~160s      │  ~2.5 GB     │
└──────────────────────┴──────────────┴─────────────┴──────────────┘


KEY FEATURES
═══════════════════════════════════════════════════════════════════════════════

✅ 7 Total Presets (3 single + 4 combined)
✅ Automatic performance warnings for combined models
✅ Dynamic UI visibility (show relevant model dropdowns)
✅ Feature concatenation for combined models
✅ Graceful fallback (if one model fails, others continue)
✅ timm NOT compiled with TorchScript
✅ Processing time estimates
✅ Memory usage warnings
✅ Backward compatible with existing configs


BENEFITS OF COMBINED MODELS
═══════════════════════════════════════════════════════════════════════════════

✅ Higher Accuracy:     Multiple perspectives on same image
✅ Robustness:          Less likely to misclassify
✅ Versatility:         Works better across diverse image types
✅ Confidence:          Higher confidence in predictions

Trade-offs:
⚠️  Slower Processing:  2-3x slower than single models
⚠️  Higher Memory:      Requires more RAM
⚠️  More Complex:       Harder to debug issues


╔══════════════════════════════════════════════════════════════════════════════╗
║                   IMPLEMENTATION COMPLETE - READY FOR PR #168                ║
╚══════════════════════════════════════════════════════════════════════════════╝
